# VueKit 智能组件库隔离系统使用说明

## 🎯 系统概述

VueKit 智能组件库隔离系统是一个创新的解决方案，用于解决多组件库共存时的组件提示混乱问题。该系统通过智能检测项目依赖和用户配置，自动过滤和排序组件，确保用户只看到相关的组件。

## ✨ 核心特性

### 1. 智能组件库隔离
- **自动检测**：根据项目 `package.json` 自动识别使用的组件库
- **动态过滤**：根据项目配置动态隐藏不相关的组件库
- **智能排序**：项目使用的组件库优先显示

### 2. 用户配置驱动
- **项目级配置**：每个项目可以有不同的组件库配置
- **全局默认配置**：设置默认启用的组件库
- **实时生效**：配置变更立即生效，无需重启

### 3. 配置管理
- **导入/导出**：支持配置文件的导入和导出
- **一键重置**：快速重置为全局默认配置
- **配置验证**：自动验证配置的有效性

## 🚀 快速开始

### 1. 打开配置界面

**方法一：通过菜单**
1. 在 IntelliJ IDEA 中打开项目
2. 点击菜单：`Tools` → `VueKit` → `组件库配置`

**方法二：通过快捷键**
- 默认快捷键：`Ctrl+Shift+V` (可自定义)

**方法三：通过项目视图**
1. 在项目视图中右键点击项目根目录
2. 选择 `VueKit` → `组件库配置`

### 2. 配置组件库

在配置界面中，您可以看到：

- **项目信息**：显示当前项目名称和全局配置路径
- **组件库列表**：所有可用的组件库，带有复选框
- **操作按钮**：导入、导出、重置、检测依赖等

#### 启用/禁用组件库
1. 勾选要启用的组件库
2. 取消勾选要禁用的组件库
3. 点击"保存"按钮

#### 推荐配置
- **Element UI 项目**：只启用 `Element UI`
- **Element Plus 项目**：只启用 `Element Plus`
- **混合项目**：根据实际需要启用多个组件库

### 3. 配置导入/导出

#### 导出配置
1. 点击"导出配置"按钮
2. 选择保存位置和文件名
3. 配置文件将保存为 JSON 格式

#### 导入配置
1. 点击"导入配置"按钮
2. 选择要导入的配置文件
3. 系统将自动应用导入的配置

## 🔧 高级配置

### 1. 项目级配置文件

您可以在项目根目录创建 `.vuekit-libraries.json` 文件：

```json
{
  "projectId": "your-project-id",
  "projectName": "Your Project Name",
  "enabledLibraries": [
    "ELEMENT_UI",
    "ELEMENT_PLUS"
  ]
}
```

### 2. 全局配置文件

全局配置文件保存在 `~/.vuekit/vuekit-libraries.json`：

```json
{
  "defaultEnabledLibraries": [
    "ELEMENT_UI",
    "ELEMENT_PLUS",
    "ANT_DESIGN_VUE"
  ]
}
```

### 3. 支持的组件库类型

| 组件库类型 | 枚举值 | 说明 |
|------------|--------|------|
| Element UI | `ELEMENT_UI` | 基于 Vue 2.x 的桌面端组件库 |
| Element Plus | `ELEMENT_PLUS` | 基于 Vue 3.x 的桌面端组件库 |
| Ant Design Vue | `ANT_DESIGN_VUE` | 基于 Ant Design 设计体系的 Vue 组件库 |
| Vuetify | `VUETIFY` | 基于 Material Design 的 Vue 组件库 |
| Quasar | `QUASAR` | 基于 Vue 的跨平台 UI 框架 |

## 📊 工作原理

### 1. 组件库检测流程

```
项目打开 → 检测 package.json → 识别依赖组件库 → 加载组件数据 → 应用智能过滤 → 显示过滤后的组件
```

### 2. 智能过滤逻辑

1. **项目依赖检测**：分析 `package.json` 中的 `dependencies` 和 `devDependencies`
2. **用户配置合并**：结合用户手动启用的组件库
3. **组件过滤**：只保留启用组件库中的组件
4. **智能排序**：项目依赖的组件库优先显示

### 3. 配置优先级

1. **项目级配置**：最高优先级，覆盖全局配置
2. **全局默认配置**：中等优先级，作为项目配置的默认值
3. **系统默认配置**：最低优先级，作为最后的备用方案

## 🎨 使用场景

### 1. 单一组件库项目

**场景**：项目只使用 Element UI
**配置**：只启用 `ELEMENT_UI`
**效果**：补全时只显示 Element UI 的组件

### 2. 多组件库项目

**场景**：项目同时使用 Element UI 和 Ant Design Vue
**配置**：启用 `ELEMENT_UI` 和 `ANT_DESIGN_VUE`
**效果**：补全时显示两个组件库的组件，按优先级排序

### 3. 组件库迁移

**场景**：从 Element UI 迁移到 Element Plus
**配置**：先启用两个组件库，逐步禁用 Element UI
**效果**：平滑过渡，避免突然的组件丢失

## 🔍 故障排除

### 1. 常见问题

#### 问题：配置界面无法打开
**解决方案**：
- 确保项目已正确打开
- 检查 VueKit 插件是否正确安装
- 重启 IntelliJ IDEA

#### 问题：配置保存失败
**解决方案**：
- 检查项目目录的写入权限
- 确保至少选择一个组件库
- 查看错误日志获取详细信息

#### 问题：组件仍然显示混乱
**解决方案**：
- 确认配置已正确保存
- 检查组件库检测是否正常
- 尝试重新加载项目

### 2. 日志查看

在 IntelliJ IDEA 中查看日志：
1. 菜单：`View` → `Tool Windows` → `Event Log`
2. 搜索关键词：`VueKit`、`组件库`、`智能过滤`

### 3. 调试模式

启用调试模式获取详细信息：
1. 在配置界面中点击"检测项目依赖"
2. 查看控制台输出的详细信息
3. 检查项目根目录的配置文件

## 📈 性能优化

### 1. 缓存策略

- **组件数据缓存**：避免重复加载组件数据
- **配置缓存**：项目配置缓存在内存中
- **智能预加载**：根据项目类型预加载相关组件库

### 2. 懒加载

- **按需加载**：只在需要时加载组件库
- **增量更新**：只更新变更的配置部分
- **后台处理**：配置变更在后台异步处理

### 3. 内存管理

- **组件对象池**：重用组件对象，减少内存分配
- **弱引用**：使用弱引用避免内存泄漏
- **定期清理**：定期清理无用的缓存数据

## 🔮 未来规划

### 1. 即将推出的功能

- **智能推荐**：根据项目特征推荐最佳组件库配置
- **配置同步**：支持团队配置的云端同步
- **性能分析**：提供组件库使用性能分析报告

### 2. 长期规划

- **机器学习**：使用 ML 优化组件库选择
- **社区配置**：支持社区共享的配置模板
- **插件生态**：支持第三方组件库的扩展

## 📞 技术支持

### 1. 获取帮助

- **文档中心**：查看完整的在线文档
- **问题反馈**：在 GitHub 上提交 Issue
- **社区讨论**：参与社区讨论获取帮助

### 2. 联系方式

- **GitHub**：https://github.com/your-org/vuekit
- **邮箱**：support@vuekit.com
- **QQ 群**：123456789

---

**版本信息**：VueKit 2.0.0  
**最后更新**：2024年12月  
**文档状态**：持续更新中
